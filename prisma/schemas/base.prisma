generator client {
    provider    = "prisma-client"
    output      = "../libs/shared/prisma"
    engineType  = "client" // enable Prisma ORM without Rust
}

datasource db {
    provider  = "postgresql"
}
