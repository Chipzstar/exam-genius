/** PostHog feature flag key — create in PostHog UI / API as boolean flag (default off). */
export const POSTHOG_FEATURE_ENABLE_EXAM_LEVEL_SELECTION = 'enable_exam_level_selection' as const;

/** PostHog feature flag key — gates async diagram/SVG generation flow (default off). */
export const POSTHOG_FEATURE_ENABLE_FIGURE_GENERATION = 'enable_figure_generation' as const;
